# v0.8.2 — Fix for large transcript events

A targeted fix for session indexing on very large Codex transcripts.

## Fixed

- **Indexing no longer fails on oversized transcript lines.** Raised the
  internal buffer limit so a single very large event in a Codex transcript
  no longer breaks indexing.

## Install

```bash
npm install -g pallium
pallium version
```
