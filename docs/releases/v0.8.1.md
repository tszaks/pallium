# v0.8.1 — Live session reader sync

A small follow-up to keep live session reading working against the latest
Codex session format.

## Fixed

- **Live session reading stays current.** Synced the live session reader
  against the newest Codex CLI session format.
- **Older Codex versions still work.** Kept compatibility with Codex state
  databases that don't have a logs table.

## Install

```bash
npm install -g pallium
pallium version
```
